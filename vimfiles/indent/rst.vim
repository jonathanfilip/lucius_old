" Vim indent file
" Language:         reStructuredText Documentation Format
" Maintainer:       Jonathan Filip
" Latest Revision:  2009-02-01

" I don't like the default indent for reStructuredText, so I just overwrite it
" with a blank file.

" Only load this indent file when no other was loaded.
if exists("b:did_indent")
  finish
endif
let b:did_indent = 1

